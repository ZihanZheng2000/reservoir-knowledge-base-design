# Legacy Retrieval-Indexing Protocol

This compatibility file is retained for old references. The current workflow
uses `skills/indexing/` for offline encoding only. It does not prescribe
retrieval queries, benchmark questions, or retrieval evaluation.
