# Validation

This folder stores cross-run validation summaries, sampled review results, and transfer validation notes.

Per-run validation files should stay inside `runs/<run_id>/validation/`. Use this folder for comparisons across runs or reservoirs.

